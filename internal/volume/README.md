# Volume Domain

This directory is reserved for volume resource logic.

Planned layout:

- `internal/volume/domain/` for volume types/interfaces
- `internal/volume/providers/` for provider volume implementations
- `internal/volume/services/` for volume workflows
- `internal/volume/tui/` for volume interactive flows
